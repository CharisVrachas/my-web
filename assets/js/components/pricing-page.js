// /pricing used to have a Personal/Business price toggle here
// (initPricingToggle) — placeholder scaffolding for numbers that didn't
// exist yet. Real pricing turned out to be a flat per-tier commission with
// nothing to toggle between, so the toggle (markup, this function, and its
// CSS in pricing-page.css) was removed along with it rather than left as
// dead code with nothing left to call it. This file is intentionally empty;
// no longer loaded from Layout.astro.
